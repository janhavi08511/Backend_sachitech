services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sachitech-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data: